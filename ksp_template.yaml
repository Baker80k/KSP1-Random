name: Baker

game: Kerbal Space Program

Kerbal Space Program:
  # Win condition.
  # Set one value to 1 (or any positive number) and the rest to 0 to pick exclusively.
  # Split the weight across multiple options to randomize between them.
  goal:
    full: 1       # All 9 KSC buildings at max level AND flags on all solid bodies
    flags_only: 0 # Flags on all solid bodies only (no building requirement)
